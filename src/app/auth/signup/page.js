import SignupForm from '../components/SignupForm';

export const metadata = {
  title: 'Enlist Now - CosmicTwin',
  description: 'Join the CosmicTwin Mission Crew',
};

export default function SignupPage() {
  return <SignupForm />;
}
